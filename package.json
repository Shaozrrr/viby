{
  "name": "viby",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check script.js && node --check works.js",
    "selftest:auth": "bash scripts/selftest-auth.sh",
    "patch:oauth-toast": "node scripts/patch-oauth-toast.cjs",
    "pm2:start": "pm2 start ecosystem.config.cjs",
    "pm2:restart": "pm2 restart viby",
    "deploy:vps": "bash scripts/deploy-from-local.sh",
    "git:pull": "git pull origin main",
    "git:push": "git push origin HEAD",
    "git:ship": "git pull --rebase origin main && git push origin HEAD",
    "publish": "bash scripts/publish-all.sh"
  },
  "engines": {
    "node": ">=18"
  }
}
